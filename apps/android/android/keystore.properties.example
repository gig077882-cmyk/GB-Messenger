# Copy to keystore.properties and keep the copy local (it is gitignored).
# Paths are relative to apps/android/android.
storeFile=app/krug-release.jks
storePassword=replace-with-a-strong-secret
keyAlias=krug-release
keyPassword=replace-with-a-strong-secret
